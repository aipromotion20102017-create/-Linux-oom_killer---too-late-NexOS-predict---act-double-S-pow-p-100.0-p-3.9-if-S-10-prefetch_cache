# -Linux-oom_killer---too-late-NexOS-predict---act-double-S-pow-p-100.0-p-3.9-if-S-10-prefetch_cache
// Linux: oom_killer -> too late // NexOS: predict -> act double S = pow(p/(100.0-p), 3.9); if (S > 10) prefetch_cache(key); if (S > 100) pin_in_ram(key);
